services:
  - type: web
    name: elite-webinar-bot
    runtime: node
    buildCommand: npm install && npx playwright install chromium
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
